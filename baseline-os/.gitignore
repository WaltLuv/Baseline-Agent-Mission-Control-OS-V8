# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.browser-use-venv/
.tmp/
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*
# …but the sanitized example template SHOULD ship in the repo
!.env.local.example
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Generated by scripts/aggregate.ts — contains real machine data.
# live-data.example.json is the committed sanitized template.
src/data/live-data.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
claude-os
.antigravitycli/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# API Keys / Credentials Manager — local file safety net
# The actual store lives at ~/.claude-os/credentials.local.json (outside the
# repo) but this guards against any *.local.json sneaking in via copy-paste.
credentials.local.json
*.credentials.local.json

# Tauri (Flight Deck desktop) build artifacts
src-tauri/target/
src-tauri/gen/
